This folder contains small maintenance scripts for the admin panel.

- `patch-size-sensor.cjs`: Applies a safety patch to `size-sensor@1.0.2` used by `echarts-for-react` to prevent a `disconnect` TypeError during unmount in React dev/StrictMode.
